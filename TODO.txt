simp::gitlab_lint fails with latest files and now requires a token

output fields of gpg command: https://github.com/CSNW/gnupg/blob/master/doc/DETAILS

File '/home/build_user/test--pkg-signrpms/test.rpms/testpackage-1-0.noarch.rpm.sig' exists. OOPs
OOPs
OOPs
Overwrite? (y/N) OOPs
Enter new filename: OOPs

gpg-related problems
- pinentry pkg::signrpms
- why use --pinentry-program (or the like) when using --batch mode?
- extracting key expiry timestamp should use --with-colons output
- don't hide socket-too-long error messages
- Why create run script on EL8 if we don't need it?
- Need to test multiple RPMs to verify doesn't kill gpg-agent unnecessarily?
  - how does the gpg-agent work throughout the project?


rpm --eval '%{__gpg_sign_cmd}'


Used only on EL7
run_gpg_agent:
#!/bin/sh

gpg-agent --homedir=/home/enemsick/lrep/test-signrpms/build/distributions/CentOS/7/x86_64/build_keys/dev --daemon \
          --no-use-standard-socket \
          --sh \
          --batch \
          --write-env-file "gpg-agent-info.env"  \
          --pinentry-program /usr/bin/pinentry-curses \
  < /dev/null &

parses gpg-agent-info.env file
          --no-use-standard-socket \        # NOT AVALAILABLE EL8: always uses standard place

EL8
%x{gpg-agent --homedir=#{Dir.pwd} >&/dev/null || gpg-agent --homedir=#{Dir.pwd} --daemon >&/dev/null

  --extra-socket none

